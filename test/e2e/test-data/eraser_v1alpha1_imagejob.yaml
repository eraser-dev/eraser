apiVersion: eraser.sh/v1alpha1
kind: ImageJob
metadata:
  name: imagejob-sample
spec:
  imageListName: imagelist-sample
  template:
    spec:
      containers:
      - name: eraser
        image: ghcr.io/azure/eraser:v0.1.0
      restartPolicy: OnFailure
