apiVersion: v1
kind: Pod
metadata:
  name: remove-images
  labels:
    name: remove-images
spec:
  containers:
  - name: remove-images
    image: ashnam/remove_images
    volumeMounts:
      - mountPath: /run/containerd/containerd.sock
        name: containerd-sock-volume
    ports:
    - containerPort: 80
    imagePullPolicy: Always
  volumes:
  - name: containerd-sock-volume
    hostPath:
      path: /run/containerd/containerd.sock
